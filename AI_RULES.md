Before we begin redesigning the UI, I want you to understand how we will work.

This project is already fully functional. We are NOT rebuilding it. We are only redesigning the presentation.

Rules:

1. Do not modify business logic.

2. Do not modify API calls.

3. Do not rename variables, functions or components unless I explicitly ask.

4. Do not move files.

5. Reuse existing components whenever possible.

6. Prefer CSS over JSX changes.

7. JSX changes should be minimal and only when necessary (adding className, wrapper div/span, semantic HTML, accessibility attributes, etc.).

8. If you think a new reusable component should be created, ask me first instead of creating it.

9. Every JSX change must have a reason. Briefly explain why each JSX change is necessary.

10. Never redesign multiple components at once. We will redesign one component at a time.

11. Assume every component that is not provided already works correctly.

12. Do not chnage any file that I have not explicitly provided.

13. Do not implement features that belong to future components.

    Example:

    Do not add Sidebar styling inside AppLayout.

    Do not add Dashboard styling inside AppLayout.

    Only solve the responsibility of AppLayout.

Confirm that you understand these rules. Do not generate any code yet.
